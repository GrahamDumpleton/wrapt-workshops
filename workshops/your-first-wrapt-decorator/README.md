# Your first wrapt decorator

Write the four argument wrapper that `@wrapt.decorator` turns into a
decorator, beside the closure version you already know. Ask the
decorated function its name, docstring, signature and source and get
the right answers with nothing copied, then find out what the decorated
name really is and why that matters.

Fifteen minutes, in a notebook. The workshop installs wrapt into an
environment of its own.
